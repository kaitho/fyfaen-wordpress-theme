# Branches

- `main`: stable theme code.
- `development`: active implementation work before changes are promoted to `main`.

For the initial visual build, work from `development` and keep production deployment separate.
